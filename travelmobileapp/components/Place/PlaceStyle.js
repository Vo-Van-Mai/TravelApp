import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container:{
        margin: 10,
        width: 200,
        borderColor: "red",
        borderWidth: 1,
        borderRadius: 10,
    },
    
    bgColor: {
        backgroundColor: "#aedaff",
    }
})