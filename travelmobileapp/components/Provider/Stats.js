import { View } from "react-native";
import Header from "../Header/Header";

const Stats = () => {
    return(
        <View>
            <Header title={"Thống kê nhà cung cấp!"} />
        </View>
    );
}

export default Stats;